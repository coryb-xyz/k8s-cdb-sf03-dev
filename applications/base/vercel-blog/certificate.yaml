apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vercel-blog-tls-cert
  namespace: ${release_env}-vercel-blog
spec:
  secretName: vercel-blog-tls
  issuerRef:
    name: letsencrypt-${release_env}
    kind: ClusterIssuer
  dnsNames:
    - "${vercel_blog_domain}"
