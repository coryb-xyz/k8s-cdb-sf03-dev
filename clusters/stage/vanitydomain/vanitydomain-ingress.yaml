apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vanitydomain-ingress
spec:
  tls:
  - hosts:
    - www.stage.1a4.co
    secretName: vanitydomain-tls
  ingressClassName: nginx
  rules:
    - host: www.stage.1a4.co
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: vanitydomain
                port:
                  number: 80

